// 200
const success = 200;

// 400
const invalid = 400;
const badRequest = 400;
const unauthorized = 401;
const noData = 404;
const maximum = 455;

// 500
const error = 500;
module.exports = {
    // 200
    success,

    //400
    invalid, 
    badRequest,
    unauthorized,
    noData,
    maximum,
    
    // 500
    error
};